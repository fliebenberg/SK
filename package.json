{
  "name": "sk",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "release": "commit-and-tag-version",
    "release:push": "npm run release && git push --follow-tags"
  },
  "devDependencies": {
    "commit-and-tag-version": "^12.6.1"
  }
}