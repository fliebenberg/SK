require('dotenv').config({ path: '../.env' }); const { Client } = require('pg'); const client = new Client({ connectionString: process.env.DATABASE_URL || 'postgresql://postgres:postgres@localhost:5432/sk' }); client.connect().then(async () => { const res = await client.query('SELECT * FROM game_disputes WHERE status=''OPEN'''); console.log(res.rows); }).catch(console.error).finally(() => client.end());